<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Google Maps and Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smartpark">

        <!-- Google Maps API Key - REQUIRED for the app not to crash -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAQLPxYBjoGlFGvSzD6g6HqqtHVi5DTs0M" />

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <activity
            android:name=".OwnerHomeActivity"
            android:exported="false" />

        <activity
            android:name=".OwnerDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".OwnerSpotActivity"
            android:exported="false" />

        <activity
            android:name=".OwnerHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".OwnerRequestsActivity"
            android:exported="false" />

        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".AdminReportsActivity"
            android:exported="false" />

        <activity
            android:name=".DriverFavoritesActivity"
            android:exported="false" />

        <activity
            android:name=".AddParkingSpotActivity"
            android:exported="false" />

        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".OwnerProfileActivity"
            android:exported="false" />

    </application>

</manifest>